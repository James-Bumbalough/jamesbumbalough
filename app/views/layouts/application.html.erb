<!DOCTYPE html>
<html lang="en">

  <head>

      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <meta name="description" content="Columbus Web Developer">
      <meta name="keywords" content="Columbus, Web Developer, Portfolio, James, Bumbalough, Full Stack Developer, Ruby, Rails, Bootstrap, Angular"
      <meta name="author" content="James Bumbalough">

      <title>James Bumbalough - Web Developer</title>

      <!-- Application CSS -->
      <%=  stylesheet_link_tag "application", media: "screen", "data-turbolinks-track" => true %>

      <!-- Custom Fonts -->
      <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
      <link href="https://fonts.googleapis.com/css?family=Lato:400,700,400italic,700italic" rel="stylesheet" type="text/css">

      <%= yield(:header) if @content_for_header %>
      <%= csrf_meta_tags %>
      <%= render 'layouts/shim' %>

  </head>
  <body>
      <%= render 'layouts/nav' %>
      <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%= render 'layouts/scrolltop' %>

  <!-- Application JavaScript -->
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

  </body>

</html>